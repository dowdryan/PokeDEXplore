function FavoritePokemon({ pokemon }) {
    return (
        <input className="Favorite-Pokemon"
                        type="checkbox"
                        checked={}
                        onChange={}
                        onMouseEnter={() => setIsHovered(true)}
                        onMouseLeave={() => setIsHovered(false)} />
    )
}